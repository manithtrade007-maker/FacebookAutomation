# Railway build config — installs ffmpeg + Python deps
[phases.setup]
nixPkgs = ["ffmpeg", "python311"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "python3 dashboard/app.py"
